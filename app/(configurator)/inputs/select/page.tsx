import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Select | MUI Configurator",
	description: "Configuration Tool for MUI Components. Built by zacaw99",
};

const Page = () => {
	return <div>Select Page</div>;
};

export default Page;
