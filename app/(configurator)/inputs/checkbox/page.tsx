import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Checkbox | MUI Configurator",
	description: "Configuration Tool for MUI Components. Built by zacaw99",
};

const Page = () => {
	return <div>Checkbox Page</div>;
};

export default Page;
