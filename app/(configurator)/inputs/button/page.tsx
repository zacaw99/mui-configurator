import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Button | MUI Configurator",
	description: "Configuration Tool for MUI Components. Built by zacaw99",
};

const Page = () => {
	return <div>Button Page</div>;
};

export default Page;
