import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Autocomplete | MUI Configurator",
	description: "Configuration Tool for MUI Components. Built by zacaw99",
};

const Page = () => {
	return <div>Autocomplete Page</div>;
};

export default Page;
