import { Metadata } from "next";

export const metadata: Metadata = {
	title: "MUI Configurator Home",
	description: "Configuration Tool for MUI Components. Built by zacaw99",
};

const Page = () => {
	return (
		<div>
			<h1>Home</h1>
		</div>
	);
};

export default Page;
